apiVersion: apps/v1
kind: Deployment
metadata:
  name: cortex-worker
  namespace: cortex-monolith
  labels:
    app: cortex-worker
    app.kubernetes.io/component: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cortex-worker
  template:
    metadata:
      labels:
        app: cortex-worker
        app.kubernetes.io/component: worker
    spec:
      enableServiceLinks: false
      containers:
        - name: cortex-worker
          image: cortex/cortex-worker:latest
          imagePullPolicy: Never
          envFrom:
            - configMapRef:
                name: cortex-config
          env:
            - name: SERVICE
              value: cortex-worker
            - name: NEO4J_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: cortex-secrets
                  key: NEO4J_PASSWORD
