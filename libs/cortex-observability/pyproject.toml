[project]
name = "cortex-observability"
version = "0.1.0"
description = "Metrics, tracing, and logging hooks for Cortex AI"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cortex_observability"]
